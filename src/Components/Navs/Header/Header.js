import React from 'react'

function Header() {
    return (
        <header>
            <h1>Clicky Game</h1>
            <h2>Click on an image but not the same one twice!</h2>
        </header>
    )
}

export default Header;
